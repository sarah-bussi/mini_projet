window.TESTIMONIALS_CONFIG = Object.freeze({
  // Valeurs publiques fournies dans Supabase > Project Settings > API.
  // La clé anon est conçue pour être exposée côté navigateur. Ne jamais placer la clé service_role ici.
  supabaseUrl: "",
  supabaseAnonKey: "",
});
